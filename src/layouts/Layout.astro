---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  /** Path to the social-share image (relative to site root). */
  image?: string;
}

const SITE_NAME = 'Patrik Megerle';

const {
  title = 'Otkrij koju vrstu boli imaš u leđima',
  description = 'Fizioterapeut Patrik Megerle — kviz za bol u leđima: je li tvoja bol vjerojatno mišićna ili problem s diskom (diskus hernija)? Saznaj u par pitanja.',
  image = '/og-image.jpg',
} = Astro.props;

// Absolute URLs (required by social scrapers). Astro.site = https://fiziomegerle.com
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const imageURL = new URL(image, Astro.site);
const faviconHref = `${import.meta.env.BASE_URL}/favicon.svg`.replace(
  /\/{2,}/g,
  '/',
);

// Full <title> includes the brand for SEO; og:title stays clean.
const pageTitle = `${title} · ${SITE_NAME}`;

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: SITE_NAME,
  url: 'https://fiziomegerle.com/',
  inLanguage: 'hr',
  description,
  author: {
    '@type': 'Person',
    name: 'Patrik Megerle',
    jobTitle: 'Fizioterapeut',
    url: 'https://fiziomegerle.com/',
    sameAs: ['https://www.instagram.com/patrik_megerle/'],
  },
};
---

<!doctype html>
<html lang="hr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />
    <meta name="robots" content="index, follow" />
    <meta name="theme-color" content="#6b705c" />
    <meta name="generator" content={Astro.generator} />
    <link rel="icon" type="image/svg+xml" href={faviconHref} />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={SITE_NAME} />
    <meta property="og:locale" content="hr_HR" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:image" content={imageURL} />
    <meta property="og:image:secure_url" content={imageURL} />
    <meta property="og:image:type" content="image/jpeg" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta
      property="og:image:alt"
      content="Patrik Megerle — kviz: Otkrij koju vrstu boli imaš u leđima"
    />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={imageURL} />
    <meta
      name="twitter:image:alt"
      content="Patrik Megerle — kviz: Otkrij koju vrstu boli imaš u leđima"
    />

    <script
      type="application/ld+json"
      set:html={JSON.stringify(jsonLd)}
    />
  </head>
  <body>
    <slot />
  </body>
</html>
